
#Configure the build directory (one-time or when you want to reconfigure)

# Do the following, or you can right click the
# CMakeLists.txt file and select "Reload CMake Project" in your IDEs

rm -rf cmake-build-debugsss
cmake -S . -B cmake-build-debug